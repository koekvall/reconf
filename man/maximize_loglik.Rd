% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{maximize_loglik}
\alias{maximize_loglik}
\title{Maximize log-likelihood with respect to specified parameters}
\usage{
maximize_loglik(
  start_val,
  opt_idx,
  Y,
  X,
  Z,
  Hlist,
  expected = TRUE,
  REML = TRUE,
  precomp = NULL,
  rinit = 1,
  rmax = 100,
  parscale = NULL,
  warn_nonconv = TRUE,
  check = TRUE,
  ...
)
}
\arguments{
\item{start_val}{Numeric vector of starting parameter values. If
\code{REML = FALSE}, this should be of length \eqn{p + r} with
\code{start_val = c(beta, psi)}, where \eqn{p} is the number of fixed
effects and \eqn{r} is the number of variance parameters. If
\code{REML = TRUE}, this should be of length \eqn{r} with
\code{start_val = psi}.}

\item{opt_idx}{Integer vector specifying which elements of \code{start_val}
to optimize. All other parameters are held fixed at their starting values.
Must have length > 0 and contain unique positive integers not exceeding
\code{length(start_val)}.}

\item{Y}{Numeric vector of length \eqn{n} containing the response values.}

\item{X}{Numeric matrix of size \eqn{n \times p} containing fixed effect
predictors.}

\item{Z}{Sparse matrix of size \eqn{n \times q} containing the random effect
design matrix.}

\item{Hlist}{List of sparse matrices determining how \eqn{\psi} is mapped to
the covariance matrix \eqn{\Psi} (see \code{?loglikelihood}).}

\item{expected}{Logical. If \code{TRUE}, use expected Fisher information
matrix; otherwise use observed information. Default is \code{TRUE}.}

\item{REML}{Logical. If \code{TRUE}, use restricted maximum likelihood;
otherwise use maximum likelihood. Default is \code{TRUE}.}

\item{precomp}{List or \code{NULL} containing precomputed quantities to speed
up computation (see \code{?get_precomp}). If \code{NULL}, quantities are
computed internally.}

\item{rinit}{Initial trust-region radius passed to \code{\link[trust]{trust}}.
Default is 1. With the default \code{parscale}, radii are lengths in the
diagonal information metric at \code{start_val}, so 1 is roughly one
standard error.}

\item{rmax}{Maximum trust-region radius passed to \code{\link[trust]{trust}}.
Default is 100, in the same units as \code{rinit}.}

\item{parscale}{Numeric vector of positive scaling factors for the free
parameters, passed to \code{\link[trust]{trust}}; the trust region is
\eqn{\|diag(parscale) s\| \le r} in the step \eqn{s}. If \code{NULL}
(default), the square root of the diagonal of the information matrix at
\code{start_val} is used, so the region approximates a ball in the
information metric; if that diagonal is not finite and positive, no
rescaling is done.}

\item{warn_nonconv}{Logical. If \code{TRUE} (default), a warning is issued
when the trust-region optimizer does not converge. Set to \code{FALSE}
when non-convergence is expected by design (e.g., when \code{iterlim = 1L}
is used for a one-step update).}

\item{check}{If \code{TRUE} (default), validate arguments. Internal callers
in loops set \code{FALSE} to skip redundant validation.}

\item{...}{Additional arguments passed to \code{\link[trust]{trust}} optimizer,
such as tolerance settings or iteration limits.}
}
\value{
A list with components:
\item{arg}{Numeric vector of optimized parameter values. Parameters not in
\code{opt_idx} retain their starting values from \code{start_val}.}
\item{value}{The maximized log-likelihood value.}
\item{conv}{Logical indicating whether the optimization converged.}
\item{iter}{Integer giving the number of iterations performed.}
}
\description{
Optimizes a subset of parameters in a linear mixed effects model while
holding others fixed. Uses the trust region algorithm for optimization.
}
\seealso{
\code{\link{score_stat}},
\code{\link{loglikelihood}}, \code{\link[trust]{trust}}
}
\keyword{internal}
